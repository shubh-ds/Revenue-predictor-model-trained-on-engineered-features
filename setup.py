from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project to predict revenue using an ML model trained on engineered features',
    author='Shubham',
    license='MIT',
)
